package com.icbc.valuation.utils;

import org.junit.Test;

import static org.junit.jupiter.api.Assertions.*;

public class ValuationUtilTest {
    @Test
    void transToScoresByConfigTest() {
        //        uploadData.clear();

    /* // 根据配置得出分数
        ValuationConfigs valuationConfigs = new ValuationConfigs();
        Map<String, Map<String, Double>> attriConfigs = new HashMap<>();
        Map<String, Double> attriMap = new HashMap<>();
        List<CompuFormula> rangeConfigs = new LinkedList<>();

        attriMap.put("起飞", 100.0);
        attriMap.put("芜湖", 200.0);
        attriConfigs.put("s1", attriMap);
        rangeConfigs.add(new CompuFormula("s2","s2 > 100? (s2 > 200 ? 200 : 100) : 0"));
        valuationConfigs.setAttriConfigs(attriConfigs);
        valuationConfigs.setRangeConfigs(rangeConfigs);

        ValuationUtil.transToScoresByConfig(uploadData, attriConfigs, rangeConfigs);*/
    }

}